@model IEnumerable<BookStore.Models.CHUDE>


<div class="lead bg-primary text-danger" style="text-align:center" ><strong class="text-danger">CATEGORIES</strong></div>
    <div class="list-group">
        @foreach (var item in Model)
        {
            <a href="@Url.Action("SPTheoChuDe","Store", new { id = item.MaCD})" class="list-group-item" style="font-size:15px">
                @item.TenChuDe
            </a>
        }
    </div>

